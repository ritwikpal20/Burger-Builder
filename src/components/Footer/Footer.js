import classes from "./Footer.module.css";

const footer = (props) => {
    return (
        <div className={classes.Footer}>
            <p>Burger Builder | 2021</p>
        </div>
    );
};

export default footer;
